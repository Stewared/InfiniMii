<!DOCTYPE html>
<html>
    <head>
        <%
        const pageTitle = 'Error - InfiniMii';
        const pageDesc = 'If you landed here, something went wrong.';
        const canonicalUrl = baseUrl + '/search';
        const keywords = 'search mii, find mii character, mii database, mii search engine';
        %>
        <%- include('metaTags') %>
        <%- include('structuredData') %>
        <%- include('faqSchema') %>
        
        <link rel="stylesheet" href="/global.css">
        <link rel="stylesheet" href="/utilityPages.css">
    </head>
    <body>
        <%- include('globalStyle') %>
        <%- include('header') %>
        <%- include('accountDisplay') %>
        <main class="content" role="main">
            <%- include('sidebar') %>
            <section class="utility-container" aria-labelledby="search-heading">
                <form method="GET" action="/searchResults" role="search">
                    <fieldset>
                        <legend id="search-heading"><%= status || 'OOPS!' %></legend>
                        <p class="error-message"><%= message || 'An unexpected error occurred.' %></p>
                        <!-- TODO: style properly (ben pls don't kill us we loved your search page) -->
                        
                        <div class="error-actions">
                            <button onclick="history.back()" class="error-btn btn-primary">Go Back</button>
                            <a href="/" class="error-btn btn-secondary"><button>Home</button></a>
                        </div>
                    </fieldset>
                </form>
            </section>
            <%- include('featuredMiis') %>
        </main>
        <%- include('footer') %>
        <script src="/global.js"></script>
    </body>
</html>